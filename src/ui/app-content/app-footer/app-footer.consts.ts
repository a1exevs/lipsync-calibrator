import { borderSuperSmallPx, MUISpacePx } from 'src/ui/shared/styles/consts';

export const appFooterVPaddingPx = MUISpacePx;

export const appFooterButtonHeightPx = MUISpacePx * 5 + borderSuperSmallPx * 2;

export const APP_FOOTER_HEIGHT_PX = 2 * appFooterVPaddingPx + appFooterButtonHeightPx;
