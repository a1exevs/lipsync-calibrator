import { borderSuperSmallPx, MUISpacePx } from 'src/ui/common/styles/consts';

export const appFooterVPaddingPx = MUISpacePx;

export const appFooterButtonHeightPx = MUISpacePx * 5 + borderSuperSmallPx * 2;

export const appFooterHeightPx = 2 * appFooterVPaddingPx + appFooterButtonHeightPx;
