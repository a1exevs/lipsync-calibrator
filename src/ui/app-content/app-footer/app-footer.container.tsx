import React from 'react';

import AppFooter from 'src/ui/app-content/app-footer/app-footer';
import useClasses from 'src/ui/app-content/app-footer/app-footer.styles';
import MUIBox from 'src/ui/common/components/mui-box/mui-box';

const AppFooterContainer: React.FC = () => {
  // TODO hide/show logic (according to current app state) should be here

  return <AppFooter />;
};

export default AppFooterContainer;
